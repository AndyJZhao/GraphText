# @package _global_

defaults:
  - /data/cora

data:
  name: cora_tag # To be overwritten by dataset specific values.
  alias: CoraTAG
  type: tag
  dataset_path: data/${data.name}/cora_random_sbert.pt
  feat_dim: 384
  text_cutoff: 64