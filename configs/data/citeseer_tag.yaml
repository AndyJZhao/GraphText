# @package _global_

defaults:
  - /data/citeseer

data:
  name: cite_tag # To be overwritten by dataset specific values.
  alias: CiteTAG
  type: tag
  dataset_path: data/${data.name}/citeseer_random_sbert.pt
  feat_dim: 384
  text_cutoff: 64